{
  "name": "ea-pip-updater",
  "version": "0.1.0",
  "description": "CLI to fetch PyPI package metadata and push updates to an external API",
  "main": "dist/cli.js",
  "scripts": {
    "dev": "ts-node src/cli.ts",
    "build": "tsc -p .",
    "start": "node dist/cli.js",
    "test": "jest --runInBand"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "commander": "^11.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.5.0",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  }
}
